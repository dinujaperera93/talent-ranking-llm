[dependencies]
# Data
numpy = ">=1.24"
pandas = ">=2.0"
scipy = ">=1.11"

# ML / embeddings
scikit-learn = ">=1.3"
gensim = ">=4.3"
sentence-transformers = ">=2.2"
datasets = ">=2.0"
mittens = ">=0.2"

# LLM inference
torch = ">=2.0"
transformers = ">=5.6.0.dev0"
accelerate = ">=1.0"
huggingface-hub = ">=0.20"
google-generativeai = ">=0.8"

# Utilities
python-dotenv = ">=1.0"
matplotlib = ">=3.7"
nltk = ">=3.8"

# Notebook
jupytext = ">=1.16"
ipython = ">=8.0"
